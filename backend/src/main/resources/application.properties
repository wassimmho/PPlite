spring.application.name=pplite-backend
server.port=8030
# MySQL DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/pp?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=malik2005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# Crée/met à jour le schéma de la BDD au démarrage. 
# Utilisez 'validate' si le schéma existe déjà et est géré manuellement. Utilisez 'none' en production.
spring.jpa.hibernate.ddl-auto=update 
# Affiche les requêtes SQL dans la console (utile pour le débogage)
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

